# dist/output-compress/scripts — 索引

> 可攜版 SKILL 隨附腳本（workspace 版 SSoT 在 `scripts/` 與 `.claude/skills/output-compress/scripts/`，本目錄為發佈鏡像）。

| 腳本 | 用途 |
|---|---|
| `fidelity-check.py` | 失真閘（白名單元素 diff，鏡像自 SKILL scripts）|
| `usage-pacer.py` | 5h 窗配速器（鏡像）|
| `claude-usage-fetch.py` | Claude 用量抓取（可攜環境用）|
| `codex-usage-fetch.py` | Codex 用量抓取（可攜環境用）|
| `codex-handoff.py` | Codex 交接包生成（可攜環境用）|
